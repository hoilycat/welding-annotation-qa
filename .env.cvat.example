# Keep the server and SDK on the same major/minor version.
CVAT_VERSION=v2.70.0
CVAT_HOST=localhost
CVAT_PORT=8080

# Used by future REST API integration. Do not commit real credentials.
CVAT_URL=http://localhost:8080
CVAT_USERNAME=
CVAT_PASSWORD=
CVAT_ACCESS_TOKEN=
